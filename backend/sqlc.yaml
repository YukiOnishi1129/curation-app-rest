version: "2"
sql:
  - engine: "postgresql"
    queries: "./queries/"
    schema: "./migrations/"
    gen:
      go:
        package: "persistence"
        out: "./internal/infrastructure/persistence"
        sql_package: "pgx/v5"